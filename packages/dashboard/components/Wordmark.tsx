/* The Snag wordmark — clean, lowercase, static. */
export function Wordmark({ style }: { style?: React.CSSProperties }) {
  return (
    <div className="logo" style={style}>
      snag
    </div>
  );
}
