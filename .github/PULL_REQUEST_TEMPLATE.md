## What

## Why

## Checklist
- [ ] `npm run typecheck` and `npm test` pass locally
- [ ] New detector? It has positive **and negative** fixtures, and ships `defaultEnabled: false` until it clears the precision bar
- [ ] No secrets, no real session data, nothing hard-coded that belongs in `.env`
- [ ] Docs updated if behavior or configuration changed
